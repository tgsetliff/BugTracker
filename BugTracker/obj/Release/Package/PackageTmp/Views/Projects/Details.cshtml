@model BugTracker.Models.ProjectUserViewModel
@using Microsoft.AspNet.Identity;
@{
    ViewBag.Title = "Project Details";
}

<div class="container">
    <h2>Project</h2>
    <hr />
    @using (Html.BeginForm("Details", "Projects", FormMethod.Post,
    new { encType = "multipart/form-data", name = "myform" }))
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">

            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(model => model.ProjectId)
            <div class="form-group">
                @Html.LabelFor(model => model.ProjectName, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.ProjectName, new { htmlAttributes = new { @class = "form-control", disabled = "disabled" } })
                    @Html.ValidationMessageFor(model => model.ProjectName, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(model => model.AssignedUsers, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.ListBoxFor(model => model.AssignedUsers, Model.AssignedUsers as MultiSelectList, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.AssignedUsers, "", new { @class = "text-danger" })
                    @if (ViewBag.Message != null)
                    {
                        <br />
                        <div class="text-danger">@ViewBag.Message</div>
                    }
                </div>
            </div>
            @*<div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Add User(s)" class="btn btn-default" />
                </div>
            </div>*@
        </div>
    }

    <div>
        @if (User.IsInRole("Admin") || User.IsInRole("PM"))
        {<a href="@Url.Action("Edit", new { Id = Model.ProjectId })" class="btn btn-primary btn-xs">Edit</a>}       
    </div>
</div>

